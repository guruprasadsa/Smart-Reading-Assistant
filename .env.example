# Google Gemini API Key — get one at https://aistudio.google.com/
GOOGLE_API_KEY=your_google_api_key_here

# Flask configuration
FLASK_DEBUG=True

# Storage paths (defaults shown — change if needed)
CHROMA_PERSIST_DIR=./chroma_db
UPLOAD_FOLDER=./uploads

# File upload limits
MAX_FILE_SIZE_MB=10
